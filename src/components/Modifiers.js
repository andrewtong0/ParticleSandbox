import React from 'react';

export default class Modifiers extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
        <button id="isLiquid"></button>
    );
  }
}